name: subscription_guard
description: "A declarative, provider-agnostic subscription tier gating package for Flutter. Gate features, widgets, and routes based on subscription tiers with zero purchase SDK dependency."
version: 0.0.2
homepage: https://github.com/moaz126/subscription_guard
repository: https://github.com/moaz126/subscription_guard
issue_tracker: https://github.com/moaz126/subscription_guard/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
