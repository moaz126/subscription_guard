/// Built-in fallback UI displayed when a feature is locked behind a higher tier.
import 'package:flutter/widgets.dart';

class DefaultLockedWidget extends StatelessWidget {
  const DefaultLockedWidget({super.key});

  // TODO: implement default locked/paywall placeholder UI

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
