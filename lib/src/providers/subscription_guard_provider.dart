/// StatefulWidget that wraps the app and manages current tier state, callbacks, and trial info.
import 'package:flutter/widgets.dart';

class SubscriptionGuardProvider extends StatefulWidget {
  const SubscriptionGuardProvider({super.key});

  @override
  State<SubscriptionGuardProvider> createState() =>
      _SubscriptionGuardProviderState();
}

class _SubscriptionGuardProviderState
    extends State<SubscriptionGuardProvider> {
  // TODO: implement provider state and callbacks

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
